# Windows Setup

Windows users have two supported paths.

## With WSL

Use Docker Desktop with WSL integration enabled. Run `./lab` from the WSL shell.

## Without WSL

Use Docker Desktop, Git for Windows and PowerShell. Run `.\lab.ps1` from the project directory.

Ansible runs inside the `forge` control-node container in this path.
