---
- name: Update motd cache
  # Use shell to support || true (root cause fix from Molecule run error; command module does not interpret shell operators)
  ansible.builtin.shell: run-parts /etc/update-motd.d || true
  changed_when: false
  check_mode: false
